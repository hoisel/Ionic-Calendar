// added to support jspm package manager import

// Should already be required, here for clarity
require( 'angular' );
require( './js/calendar-tpls' );

// Export namespace
module.exports = 'ui.rCalendar';